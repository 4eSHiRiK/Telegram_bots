from aiogram import types


start = types.ReplyKeyboardMarkup(resize_keyboard=True)

info = types.KeyboardButton('Info')
books = types.KeyboardButton('Books')
back = types.KeyboardButton('Back')
# start.add(info)

